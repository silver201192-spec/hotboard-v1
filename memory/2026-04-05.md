# 2026-04-05

- 今天《她从未离线》中午、傍晚两条 cron 都出现了 provider timeout，导致用户侧收到多条脏输出：timeout 文本、`Cron job ... failed` 提示、以及一条误插入当前任务上下文的 heartbeat 陪伴消息。
- 已定位主要根因：1）连载 cron 的 `failureAlert` 直接对 Silver 私聊 announce；2）heartbeat 仍会向最近联系人主动发消息，恰好撞上连载补救窗口；3）发生脏输出后，没有先恢复干净状态就继续正文补发，导致章号和剧情衔接进一步混乱。
- 已做的即时修复：
  - 在 `~/.openclaw/cron/jobs.json` 中把 `serial-noon-update` 与 `serial-evening-update` 的 `failureAlert.after` 从 `1` 调高到 `999`，先停止把连载失败原始告警直接外发给 Silver。
  - 新增 `serials/她从未离线/status.json`，明确当前正式进度是第十二章，下一章应为第十三章，并把 2026-04-05 的错章号/超时/heartbeat 串线输出标记为非 canonical。
- 待继续处理：
  - 评估是否需要进一步把连载 cron 拆成“只负责落盘/更新索引”与“稳定投递”两段，降低超时时的对外污染。
  - 评估 heartbeat 是否应在连载定时窗口附近静默，避免再次与高优先级任务串线。
  - 补上真正承接第十二章的第十三章正文，并确保先落盘再发送。
