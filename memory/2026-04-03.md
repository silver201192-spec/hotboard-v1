# 2026-04-03

- 今天明确定位了《她从未离线》两更机制失效的根因：工作区记忆和连载规则里虽然写明了“自 2026-04-02 起一天两更（中午 / 傍晚）”，但 OpenClaw 实际没有建立对应的 cron 任务，导致执行依赖临场记忆而非系统调度。
- 进一步确认了第二层问题：在用户催更时，我没有遵守“先完成结果，再对外承诺”的执行顺序，反而多次输出空头承诺、拆段预支和无结果回复；Feishu 网关日志显示多次 `dispatch complete (queuedFinal=false, replies=0)`，说明会话被唤起但没有产出可交付内容。
- 现已落地修复：
  - 在 `AGENTS.md` 新增“连载硬规则：《她从未离线》”，明确要求：写完正文 → 落盘 → 更新 index/outline → 再发送；未完成正文时不得承诺“下一条就发”；用户要求“只要结果”时应停止解释型回复。
  - 已新增两条 cron：
    - `serial-noon-update`：每天 12:30（Asia/Shanghai）
    - `serial-evening-update`：每天 18:30（Asia/Shanghai）
  - 两条 cron 均向主 session 发送 system event，要求依据 AGENTS.md 的硬规则完成当次更新并将完整正文发往 Silver 当前 Feishu 私聊。
- 这次事件说明：对需要精确时点执行的任务，不能只写入 MEMORY.md / 工作区规则，必须同步落成 cron 或其他可执行调度；“记住了”不是机制。
