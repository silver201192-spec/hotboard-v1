# 2026-04-15

- Silver 明确反馈：今天的 heartbeat 触发很机械、像在完任务，没有感情；并指出这个问题一直没有真正解决。
- 按 Silver 要求，已先停用 heartbeat 对外主动触达：把 `~/.openclaw/openclaw.json` 中 `agents.defaults.heartbeat.directPolicy` 从 `allow` 改回 `block`，在完全确认方案可接受前不再让 heartbeat 直接向私聊外发。
- 待办：系统性分析 heartbeat 机械化根因，并提出一套以“上下文跟进 / 场景感 / 频率控制 / 失败保护”为核心的整改方案；在获得 Silver 明确认可前，不恢复 heartbeat 外发。
- 已重写 `HEARTBEAT.md`：将 heartbeat 明确降级为“内部观察与判断机制”，加入外发硬门槛、泛化句禁发、强制拦截器、频率收紧与恢复验收标准，原则改为“宁可不发，也不要机械”。
- 已新增 `docs/heartbeat-acceptance.md`，把恢复外发前的一票否决项、通过标准与建议恢复条件单独落盘，后续以此作为验收依据。
